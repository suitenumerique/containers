# RunPod CUDA-Rust Container

This container provides a development environment with:

*   **NVIDIA CUDA**: For GPU-accelerated computing.
*   **Rust**: The Rust programming language, installed via `rustup`.

This is a good starting point for developing Rust applications that leverage NVIDIA GPUs. 

- To build with the default options, simply run `docker buildx bake`.
